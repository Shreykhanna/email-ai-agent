import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // allowedDevOrigins: "https:// supervisually-nonsynonymous-gail.ngrok-free.dev",
};

export default nextConfig;
